#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int num;
    cout << "Please enter num: ";
    cin >> num;    
int a = pow(num,2);    
int b = pow(num,3);    
int c = pow(num,4);    
    
    cout << "Rounded area: " << floor(a) << "\n"<< floor(b)<<"\n"<< floor(c) << "\n";
    // solution Write a program to ask the user to enter: • Number
// Then Print the Number^2 , Number^3 , Number^4
 #31

    return 0;
}
